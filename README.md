# Round Builder 3D
Android build of the touch-first block-building prototype.

Controls: single tap builds, double-tap ground moves, drag rotates. Remove mode deletes a tapped block.

World state is stored locally on the device and autosaves after edits, movement, app pause, and explicit Save. Load restores the saved world.
